//The type of interpolation a projectile follows along the path to the target
public enum ProjectilePath
{
    Straight,
    Arc,
    Instant,
    ReversedStraight,
    ReversedArc,
    ReversedInstant
};
